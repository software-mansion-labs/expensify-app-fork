import type {DataSourceParam} from '@shopify/react-native-skia';

/**
 * Font source for web platform - uses URL to woff file
 */
const CHART_FONT_SOURCE: DataSourceParam = '/fonts/ExpensifyNeue-Regular.woff';

export default CHART_FONT_SOURCE;
