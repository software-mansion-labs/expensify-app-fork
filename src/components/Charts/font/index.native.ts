import type {DataSourceParam} from '@shopify/react-native-skia';

/**
 * Font source for native platforms (iOS/Android) - uses bundled otf file
 */
// eslint-disable-next-line @typescript-eslint/no-require-imports
const CHART_FONT_SOURCE: DataSourceParam = require('@assets/fonts/native/ExpensifyNeue-Regular.otf');

export default CHART_FONT_SOURCE;
