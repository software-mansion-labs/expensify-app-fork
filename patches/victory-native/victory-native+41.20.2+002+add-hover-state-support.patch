diff --git a/node_modules/victory-native/dist/cartesian/CartesianChart.d.ts b/node_modules/victory-native/dist/cartesian/CartesianChart.d.ts
index 5b718aa..e248487 100644
--- a/node_modules/victory-native/dist/cartesian/CartesianChart.d.ts
+++ b/node_modules/victory-native/dist/cartesian/CartesianChart.d.ts
@@ -5,6 +5,7 @@ import { type MutableRefObject } from "react";
 import type { ScaleLinear } from "d3-scale";
 import type { AxisProps, CartesianChartRenderArg, InputFields, NumericalFields, SidedNumber, ChartBounds, YAxisInputProps, XAxisInputProps, FrameInputProps, ChartPressPanConfig, Viewport, GestureHandlerConfig } from "../types";
 import type { ChartPressState, ChartPressStateInit } from "./hooks/useChartPressState";
+import type { ChartHoverState } from "./hooks/useChartHoverState";
 import { type ChartTransformState } from "./hooks/useChartTransformState";
 import { type PanTransformGestureConfig, type PinchTransformGestureConfig } from "./utils/transformGestures";
 export type CartesianActionsHandle<T = undefined> = T extends ChartPressState<infer S> ? S extends ChartPressStateInit ? {
@@ -35,6 +36,10 @@ type CartesianChartProps<RawData extends Record<string, unknown>, XK extends key
     chartPressConfig?: {
         pan?: ChartPressPanConfig;
     };
+    chartHoverState?: ChartHoverState<{
+        x: InputFields<RawData>[XK];
+        y: Record<YK, number>;
+    }>;
     gestureHandlerConfig?: GestureHandlerConfig;
     children: (args: CartesianChartRenderArg<RawData, YK>) => React.ReactNode;
     renderOutside?: (args: CartesianChartRenderArg<RawData, YK>) => React.ReactNode;
diff --git a/node_modules/victory-native/dist/cartesian/CartesianChart.js b/node_modules/victory-native/dist/cartesian/CartesianChart.js
index a3a842c..f817ad6 100644
--- a/node_modules/victory-native/dist/cartesian/CartesianChart.js
+++ b/node_modules/victory-native/dist/cartesian/CartesianChart.js
@@ -71,7 +71,7 @@ function CartesianChart(_a) {
     </CartesianTransformContext_1.CartesianTransformProvider>);
 }
 exports.CartesianChart = CartesianChart;
-function CartesianChartContent({ data, xKey, yKeys, padding, domainPadding, children, renderOutside = () => null, axisOptions, domain, chartPressState, chartPressConfig, gestureHandlerConfig, onChartBoundsChange, onScaleChange, gestureLongPressDelay = 100, xAxis, yAxis, frame, transformState, transformConfig, customGestures, actionsRef, viewport, ref, }) {
+function CartesianChartContent({ data, xKey, yKeys, padding, domainPadding, children, renderOutside = () => null, axisOptions, domain, chartPressState, chartPressConfig, chartHoverState, gestureHandlerConfig, onChartBoundsChange, onScaleChange, gestureLongPressDelay = 100, xAxis, yAxis, frame, transformState, transformConfig, customGestures, actionsRef, viewport, ref, }) {
     var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l;
     const [size, setSize] = React.useState({ width: 0, height: 0 });
     const chartBoundsRef = React.useRef(undefined);
@@ -381,6 +381,43 @@ function CartesianChartContent({ data, xKey, yKeys, padding, domainPadding, chil
     if ((_f = chartPressConfig === null || chartPressConfig === void 0 ? void 0 : chartPressConfig.pan) === null || _f === void 0 ? void 0 : _f.failOffsetY) {
         panGesture.failOffsetX(chartPressConfig.pan.failOffsetY);
     }
+    /**
+     * Hover gesture handling (web/desktop only)
+     */
+    const hoverGesture = react_native_gesture_handler_1.Gesture.Hover()
+        .onBegin((e) => {
+        "worklet";
+        var _a, _b;
+        if (!chartHoverState)
+            return;
+        const scrolledX = ((_a = transformState === null || transformState === void 0 ? void 0 : transformState.matrix.value) === null || _a === void 0 ? void 0 : _a[3]) || 0;
+        const scrolledY = ((_b = transformState === null || transformState === void 0 ? void 0 : transformState.matrix.value) === null || _b === void 0 ? void 0 : _b[7]) || 0;
+        const cursorX = e.x - scrolledX;
+        const cursorY = e.y - scrolledY;
+        chartHoverState.isActive.value = true;
+        chartHoverState.cursor.x.value = cursorX;
+        chartHoverState.cursor.y.value = cursorY;
+        handleTouch(chartHoverState, cursorX, cursorY);
+    })
+        .onUpdate((e) => {
+        "worklet";
+        var _a, _b;
+        if (!chartHoverState)
+            return;
+        const scrolledX = ((_a = transformState === null || transformState === void 0 ? void 0 : transformState.matrix.value) === null || _a === void 0 ? void 0 : _a[3]) || 0;
+        const scrolledY = ((_b = transformState === null || transformState === void 0 ? void 0 : transformState.matrix.value) === null || _b === void 0 ? void 0 : _b[7]) || 0;
+        const cursorX = e.x - scrolledX;
+        const cursorY = e.y - scrolledY;
+        chartHoverState.cursor.x.value = cursorX;
+        chartHoverState.cursor.y.value = cursorY;
+        handleTouch(chartHoverState, cursorX, cursorY);
+    })
+        .onEnd(() => {
+        "worklet";
+        if (!chartHoverState)
+            return;
+        chartHoverState.isActive.value = false;
+    });
     const points = React.useMemo(() => {
         const cache = {};
         return new Proxy({}, {
@@ -486,6 +523,9 @@ function CartesianChartContent({ data, xKey, yKeys, padding, domainPadding, chil
     if (chartPressState) {
         composed = react_native_gesture_handler_1.Gesture.Race(composed, panGesture);
     }
+    if (chartHoverState) {
+        composed = react_native_gesture_handler_1.Gesture.Simultaneous(composed, hoverGesture);
+    }
     return (<react_native_gesture_handler_1.GestureHandlerRootView>
       <react_native_1.View style={{ flex: 1, overflow: "hidden" }} onLayout={onLayout}>
         {body}
diff --git a/node_modules/victory-native/dist/cartesian/hooks/useChartHoverState.d.ts b/node_modules/victory-native/dist/cartesian/hooks/useChartHoverState.d.ts
new file mode 100644
index 0000000..79efcb3
--- /dev/null
+++ b/node_modules/victory-native/dist/cartesian/hooks/useChartHoverState.d.ts
@@ -0,0 +1,27 @@
+import { type SharedValue } from "react-native-reanimated";
+import type { InputFieldType } from "../../types";
+export declare const useChartHoverState: <Init extends ChartHoverStateInit>(initialValues: Init) => {
+    state: ChartHoverState<Init>;
+    isActive: boolean;
+};
+export type ChartHoverStateInit = {
+    x: InputFieldType;
+    y: Record<string, number>;
+};
+export type ChartHoverState<Init extends ChartHoverStateInit> = {
+    isActive: SharedValue<boolean>;
+    matchedIndex: SharedValue<number>;
+    x: {
+        value: SharedValue<Init["x"]>;
+        position: SharedValue<number>;
+    };
+    y: Record<keyof Init["y"], {
+        value: SharedValue<number>;
+        position: SharedValue<number>;
+    }>;
+    yIndex: SharedValue<number>;
+    cursor: {
+        x: SharedValue<number>;
+        y: SharedValue<number>;
+    };
+};
diff --git a/node_modules/victory-native/dist/cartesian/hooks/useChartHoverState.js b/node_modules/victory-native/dist/cartesian/hooks/useChartHoverState.js
new file mode 100644
index 0000000..187b32c
--- /dev/null
+++ b/node_modules/victory-native/dist/cartesian/hooks/useChartHoverState.js
@@ -0,0 +1,59 @@
+"use strict";
+var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
+    if (k2 === undefined) k2 = k;
+    var desc = Object.getOwnPropertyDescriptor(m, k);
+    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
+      desc = { enumerable: true, get: function() { return m[k]; } };
+    }
+    Object.defineProperty(o, k2, desc);
+}) : (function(o, m, k, k2) {
+    if (k2 === undefined) k2 = k;
+    o[k2] = m[k];
+}));
+var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
+    Object.defineProperty(o, "default", { enumerable: true, value: v });
+}) : function(o, v) {
+    o["default"] = v;
+});
+var __importStar = (this && this.__importStar) || function (mod) {
+    if (mod && mod.__esModule) return mod;
+    var result = {};
+    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
+    __setModuleDefault(result, mod);
+    return result;
+};
+Object.defineProperty(exports, "__esModule", { value: true });
+exports.useChartHoverState = void 0;
+const React = __importStar(require("react"));
+const react_native_reanimated_1 = require("react-native-reanimated");
+const useChartHoverState = (initialValues) => {
+    const keys = Object.keys(initialValues.y).join(",");
+    const state = React.useMemo(() => {
+        return {
+            isActive: (0, react_native_reanimated_1.makeMutable)(false),
+            matchedIndex: (0, react_native_reanimated_1.makeMutable)(-1),
+            x: { value: (0, react_native_reanimated_1.makeMutable)(initialValues.x), position: (0, react_native_reanimated_1.makeMutable)(0) },
+            y: Object.entries(initialValues.y).reduce((acc, [key, initVal]) => {
+                acc[key] = {
+                    value: (0, react_native_reanimated_1.makeMutable)(initVal),
+                    position: (0, react_native_reanimated_1.makeMutable)(0),
+                };
+                return acc;
+            }, {}),
+            yIndex: (0, react_native_reanimated_1.makeMutable)(-1), // used for stacked bar charts
+            cursor: { x: (0, react_native_reanimated_1.makeMutable)(0), y: (0, react_native_reanimated_1.makeMutable)(0) }, // raw cursor position
+        };
+        // eslint-disable-next-line react-hooks/exhaustive-deps
+    }, [keys]);
+    const isActive = useIsHoverActive(state);
+    return { state, isActive };
+};
+exports.useChartHoverState = useChartHoverState;
+const useIsHoverActive = (value) => {
+    const [isHoverActive, setIsHoverActive] = React.useState(() => value.isActive.value);
+    (0, react_native_reanimated_1.useAnimatedReaction)(() => value.isActive.value, (val, oldVal) => {
+        if (val !== oldVal)
+            (0, react_native_reanimated_1.runOnJS)(setIsHoverActive)(val);
+    });
+    return isHoverActive;
+};
diff --git a/node_modules/victory-native/dist/index.d.ts b/node_modules/victory-native/dist/index.d.ts
index 86ccba5..3430555 100644
--- a/node_modules/victory-native/dist/index.d.ts
+++ b/node_modules/victory-native/dist/index.d.ts
@@ -9,6 +9,7 @@ export { useAnimatedPath } from "./hooks/useAnimatedPath";
 export { AnimatedPath } from "./cartesian/components/AnimatedPath";
 export { usePrevious } from "./utils/usePrevious";
 export { useChartPressState, type ChartPressState, } from "./cartesian/hooks/useChartPressState";
+export { useChartHoverState, type ChartHoverState, } from "./cartesian/hooks/useChartHoverState";
 export { useChartTransformState } from "./cartesian/hooks/useChartTransformState";
 export { getTransformComponents, setScale, setTranslate, invert4, } from "./utils/transform";
 export { useLinePath } from "./cartesian/hooks/useLinePath";
diff --git a/node_modules/victory-native/dist/index.js b/node_modules/victory-native/dist/index.js
index 39dc9ac..23eeaf2 100644
--- a/node_modules/victory-native/dist/index.js
+++ b/node_modules/victory-native/dist/index.js
@@ -1,6 +1,6 @@
 "use strict";
 Object.defineProperty(exports, "__esModule", { value: true });
-exports.useSliceAngularInsetPath = exports.useSlicePath = exports.Pie = exports.PolarChart = exports.CartesianAxis = exports.Scatter = exports.useStackedAreaPaths = exports.AreaRange = exports.StackedArea = exports.Area = exports.useAreaPath = exports.BarGroup = exports.useBarGroupPaths = exports.StackedBar = exports.useStackedBarPaths = exports.Bar = exports.useBarPath = exports.Line = exports.useLinePath = exports.invert4 = exports.setTranslate = exports.setScale = exports.getTransformComponents = exports.useChartTransformState = exports.useChartPressState = exports.usePrevious = exports.AnimatedPath = exports.useAnimatedPath = exports.CartesianChart = void 0;
+exports.useSliceAngularInsetPath = exports.useSlicePath = exports.Pie = exports.PolarChart = exports.CartesianAxis = exports.Scatter = exports.useStackedAreaPaths = exports.AreaRange = exports.StackedArea = exports.Area = exports.useAreaPath = exports.BarGroup = exports.useBarGroupPaths = exports.StackedBar = exports.useStackedBarPaths = exports.Bar = exports.useBarPath = exports.Line = exports.useLinePath = exports.invert4 = exports.setTranslate = exports.setScale = exports.getTransformComponents = exports.useChartTransformState = exports.useChartHoverState = exports.useChartPressState = exports.usePrevious = exports.AnimatedPath = exports.useAnimatedPath = exports.CartesianChart = void 0;
 /**
  * Cartesian chart exports (including useful types)
  */
@@ -14,6 +14,8 @@ var usePrevious_1 = require("./utils/usePrevious");
 Object.defineProperty(exports, "usePrevious", { enumerable: true, get: function () { return usePrevious_1.usePrevious; } });
 var useChartPressState_1 = require("./cartesian/hooks/useChartPressState");
 Object.defineProperty(exports, "useChartPressState", { enumerable: true, get: function () { return useChartPressState_1.useChartPressState; } });
+var useChartHoverState_1 = require("./cartesian/hooks/useChartHoverState");
+Object.defineProperty(exports, "useChartHoverState", { enumerable: true, get: function () { return useChartHoverState_1.useChartHoverState; } });
 var useChartTransformState_1 = require("./cartesian/hooks/useChartTransformState");
 Object.defineProperty(exports, "useChartTransformState", { enumerable: true, get: function () { return useChartTransformState_1.useChartTransformState; } });
 var transform_1 = require("./utils/transform");
diff --git a/node_modules/victory-native/dist/types.d.ts b/node_modules/victory-native/dist/types.d.ts
index efb5207..49ed0f5 100644
--- a/node_modules/victory-native/dist/types.d.ts
+++ b/node_modules/victory-native/dist/types.d.ts
@@ -202,14 +202,22 @@ export type YAxisInputProps<RawData extends Record<string, unknown>, YK extends
     domain?: YAxisDomain;
     linePathEffect?: DashPathEffectComponent;
     enableRescaling?: boolean;
+    /** Controls the horizontal extent of grid lines. 'content' extends lines to content edges (e.g., bar edges), 'domain' uses full padded domain (default). */
+    lineExtent?: "domain" | "content";
+    /** Inner padding between chart elements (required for lineExtent='content'). Should match Bar component's innerPadding. */
+    contentInnerPadding?: number;
+    /** Number of data points (required for lineExtent='content'). */
+    dataPointCount?: number;
 };
-export type YAxisPropsWithDefaults<RawData extends Record<string, unknown>, YK extends keyof NumericalFields<RawData>> = Required<Omit<YAxisInputProps<RawData, YK>, "font" | "tickValues" | "linePathEffect" | "enableRescaling">> & Partial<Pick<YAxisInputProps<RawData, YK>, "font" | "tickValues" | "linePathEffect" | "enableRescaling">>;
+export type YAxisPropsWithDefaults<RawData extends Record<string, unknown>, YK extends keyof NumericalFields<RawData>> = Required<Omit<YAxisInputProps<RawData, YK>, "font" | "tickValues" | "linePathEffect" | "enableRescaling" | "lineExtent" | "contentInnerPadding" | "dataPointCount">> & Partial<Pick<YAxisInputProps<RawData, YK>, "font" | "tickValues" | "linePathEffect" | "enableRescaling" | "lineExtent" | "contentInnerPadding" | "dataPointCount">>;
 export type YAxisProps<RawData extends Record<string, unknown>, YK extends keyof NumericalFields<RawData>> = YAxisPropsWithDefaults<RawData, YK> & {
     xScale: Scale;
     yScale: Scale;
     yTicksNormalized: number[];
     yKeys: YK[];
     chartBounds: ChartBounds;
+    /** Original data bounds (before domainPadding) for lineExtent='content' */
+    xDataBounds?: [number, number];
 };
 export type FrameInputProps = {
     lineWidth?: SidedNumber;
diff --git a/node_modules/victory-native/src/cartesian/CartesianChart.tsx b/node_modules/victory-native/src/cartesian/CartesianChart.tsx
index 9b82b54..12fd5fe 100644
--- a/node_modules/victory-native/src/cartesian/CartesianChart.tsx
+++ b/node_modules/victory-native/src/cartesian/CartesianChart.tsx
@@ -35,6 +35,7 @@ import type {
   ChartPressState,
   ChartPressStateInit,
 } from "./hooks/useChartPressState";
+import type { ChartHoverState } from "./hooks/useChartHoverState";
 import { useFunctionRef } from "../hooks/useFunctionRef";
 import { CartesianChartProvider } from "./contexts/CartesianChartContext";
 import { XAxis } from "./components/XAxis";
@@ -90,6 +91,10 @@ type CartesianChartProps<
   chartPressConfig?: {
     pan?: ChartPressPanConfig;
   };
+  chartHoverState?: ChartHoverState<{
+    x: InputFields<RawData>[XK];
+    y: Record<YK, number>;
+  }>;
   gestureHandlerConfig?: GestureHandlerConfig;
   children: (args: CartesianChartRenderArg<RawData, YK>) => React.ReactNode;
   renderOutside?: (
@@ -167,6 +172,7 @@ function CartesianChartContent<
   domain,
   chartPressState,
   chartPressConfig,
+  chartHoverState,
   gestureHandlerConfig,
   onChartBoundsChange,
   onScaleChange,
@@ -238,12 +244,13 @@ function CartesianChartContent<
     chartBounds,
     isNumericalData,
     xTicksNormalized,
+    xDataBounds,
     _tData,
   } = React.useMemo(() => {
     if (!data.length) {
       return createFallbackChartState<RawData, XK, YK>(yKeys);
     }
-    const { xScale, yAxes, isNumericalData, xTicksNormalized, ..._tData } =
+    const { xScale, yAxes, isNumericalData, xTicksNormalized, xDataBounds, ..._tData } =
       transformInputData({
         data,
         xKey,
@@ -282,6 +289,7 @@ function CartesianChartContent<
       xScale,
       chartBounds,
       isNumericalData,
+      xDataBounds,
       _tData,
     };
   }, [
@@ -574,6 +582,54 @@ function CartesianChartContent<
     panGesture.failOffsetX(chartPressConfig.pan.failOffsetY);
   }
 
+  /**
+   * Hover gesture handling (web only)
+   * Uses e.x/e.y (relative to view) instead of e.absoluteX/Y (relative to screen)
+   */
+  const hoverGesture = Gesture.Hover()
+    .onBegin((e) => {
+      "worklet";
+      if (!chartHoverState) return;
+      chartHoverState.isActive.value = true;
+      const scrolledX = transformState?.matrix.value?.[3] || 0;
+      const scrolledY = transformState?.matrix.value?.[7] || 0;
+      const cursorX = e.x - scrolledX;
+      const cursorY = e.y - scrolledY;
+      chartHoverState.cursor.x.value = cursorX;
+      chartHoverState.cursor.y.value = cursorY;
+      handleTouch(
+        chartHoverState as unknown as ChartPressState<{
+          x: InputFields<RawData>[XK];
+          y: Record<YK, number>;
+        }>,
+        cursorX,
+        cursorY,
+      );
+    })
+    .onUpdate((e) => {
+      "worklet";
+      if (!chartHoverState) return;
+      const scrolledX = transformState?.matrix.value?.[3] || 0;
+      const scrolledY = transformState?.matrix.value?.[7] || 0;
+      const cursorX = e.x - scrolledX;
+      const cursorY = e.y - scrolledY;
+      chartHoverState.cursor.x.value = cursorX;
+      chartHoverState.cursor.y.value = cursorY;
+      handleTouch(
+        chartHoverState as unknown as ChartPressState<{
+          x: InputFields<RawData>[XK];
+          y: Record<YK, number>;
+        }>,
+        cursorX,
+        cursorY,
+      );
+    })
+    .onEnd(() => {
+      "worklet";
+      if (!chartHoverState) return;
+      chartHoverState.isActive.value = false;
+    });
+
   /**
    * Allow end-user to request "raw-ish" data for a given yKey.
    * Generate this on demand using a proxy.
@@ -680,6 +736,7 @@ function CartesianChartContent<
                   : rescaledTicks
               }
               chartBounds={chartBounds}
+              xDataBounds={xDataBounds}
             />
           );
         })
@@ -747,6 +804,9 @@ function CartesianChartContent<
   if (chartPressState) {
     composed = Gesture.Race(composed, panGesture);
   }
+  if (chartHoverState) {
+    composed = Gesture.Simultaneous(composed, hoverGesture);
+  }
 
   return (
     <GestureHandlerRootView>
diff --git a/node_modules/victory-native/src/cartesian/components/YAxis.tsx b/node_modules/victory-native/src/cartesian/components/YAxis.tsx
index 478f6f0..cdd6b55 100644
--- a/node_modules/victory-native/src/cartesian/components/YAxis.tsx
+++ b/node_modules/victory-native/src/cartesian/components/YAxis.tsx
@@ -27,8 +27,32 @@ export const YAxis = <
   formatYLabel = (label: ValueOf<InputDatum>) => String(label),
   linePathEffect,
   chartBounds,
+  lineExtent,
+  xDataBounds,
+  contentInnerPadding,
+  dataPointCount,
 }: YAxisProps<RawData, YK>) => {
-  const [x1 = 0, x2 = 0] = xScale.domain();
+  // Calculate line start/end positions based on lineExtent
+  const getLineXPositions = (): [number, number] => {
+    const [domainX1 = 0, domainX2 = 0] = xScale.domain();
+
+    // For 'content' mode with 2+ data points, extend lines to content edges
+    // For single data point, fall back to 'domain' mode for consistent line extent
+    if (lineExtent === "content" && xDataBounds && contentInnerPadding !== undefined && dataPointCount && dataPointCount > 1) {
+      // Lines from left edge of first element to right edge of last element
+      const domainWidth = chartBounds.right - chartBounds.left;
+      const halfElementWidth = (1 - contentInnerPadding) * domainWidth / dataPointCount / 2;
+      return [
+        xScale(xDataBounds[0]) - halfElementWidth,
+        xScale(xDataBounds[1]) + halfElementWidth,
+      ];
+    }
+
+    // Default: use full domain
+    return [xScale(domainX1), xScale(domainX2)];
+  };
+
+  const [lineX1, lineX2] = getLineXPositions();
   const [_ = 0, y2 = 0] = yScale.domain();
   const fontSize = font?.getSize() ?? 0;
   const yAxisNodes = yTicksNormalized.map((tick) => {
@@ -62,8 +86,8 @@ export const YAxis = <
         {lineWidth > 0 ? (
           <Group clip={boundsToClip(chartBounds)}>
             <Line
-              p1={vec(xScale(x1), yScale(tick))}
-              p2={vec(xScale(x2), yScale(tick))}
+              p1={vec(lineX1, yScale(tick))}
+              p2={vec(lineX2, yScale(tick))}
               color={lineColor}
               strokeWidth={lineWidth}
             >
diff --git a/node_modules/victory-native/src/cartesian/hooks/useChartHoverState.ts b/node_modules/victory-native/src/cartesian/hooks/useChartHoverState.ts
new file mode 100644
index 0000000..323e056
--- /dev/null
+++ b/node_modules/victory-native/src/cartesian/hooks/useChartHoverState.ts
@@ -0,0 +1,78 @@
+import * as React from "react";
+import {
+  makeMutable,
+  runOnJS,
+  type SharedValue,
+  useAnimatedReaction,
+} from "react-native-reanimated";
+import type { InputFieldType } from "../../types";
+
+export const useChartHoverState = <Init extends ChartHoverStateInit>(
+  initialValues: Init,
+): { state: ChartHoverState<Init>; isActive: boolean } => {
+  const keys = Object.keys(initialValues.y).join(",");
+
+  const state = React.useMemo(() => {
+    return {
+      isActive: makeMutable(false),
+      matchedIndex: makeMutable(-1),
+      x: { value: makeMutable(initialValues.x), position: makeMutable(0) },
+      y: Object.entries(initialValues.y).reduce(
+        (acc, [key, initVal]) => {
+          acc[key as keyof Init["y"]] = {
+            value: makeMutable(initVal),
+            position: makeMutable(0),
+          };
+          return acc;
+        },
+        {} as Record<
+          keyof Init["y"],
+          { value: SharedValue<number>; position: SharedValue<number> }
+        >,
+      ),
+      yIndex: makeMutable(-1), // used for stacked bar charts
+      cursor: { x: makeMutable(0), y: makeMutable(0) }, // raw cursor position
+    };
+    // eslint-disable-next-line react-hooks/exhaustive-deps
+  }, [keys]);
+
+  const isActive = useIsHoverActive(state);
+
+  return { state, isActive };
+};
+
+export type ChartHoverStateInit = {
+  x: InputFieldType;
+  y: Record<string, number>;
+};
+export type ChartHoverState<Init extends ChartHoverStateInit> = {
+  isActive: SharedValue<boolean>;
+  matchedIndex: SharedValue<number>;
+  x: {
+    value: SharedValue<Init["x"]>;
+    position: SharedValue<number>;
+  };
+  y: Record<
+    keyof Init["y"],
+    { value: SharedValue<number>; position: SharedValue<number> }
+  >;
+  yIndex: SharedValue<number>;
+  cursor: { x: SharedValue<number>; y: SharedValue<number> };
+};
+
+const useIsHoverActive = <Init extends ChartHoverStateInit>(
+  value: ChartHoverState<Init>,
+) => {
+  const [isHoverActive, setIsHoverActive] = React.useState(
+    () => value.isActive.value,
+  );
+
+  useAnimatedReaction(
+    () => value.isActive.value,
+    (val, oldVal) => {
+      if (val !== oldVal) runOnJS(setIsHoverActive)(val);
+    },
+  );
+
+  return isHoverActive;
+};
diff --git a/node_modules/victory-native/src/cartesian/utils/createFallbackChartState.ts b/node_modules/victory-native/src/cartesian/utils/createFallbackChartState.ts
index b0583cf..b606922 100644
--- a/node_modules/victory-native/src/cartesian/utils/createFallbackChartState.ts
+++ b/node_modules/victory-native/src/cartesian/utils/createFallbackChartState.ts
@@ -30,6 +30,7 @@ export function createFallbackChartState<
     chartBounds: { left: 0, right: 0, top: 0, bottom: 0 },
     isNumericalData: false,
     xTicksNormalized: [] as number[],
+    xDataBounds: [0, 0] as [number, number],
     _tData: {
       ix: [],
       ox: [],
diff --git a/node_modules/victory-native/src/cartesian/utils/transformInputData.ts b/node_modules/victory-native/src/cartesian/utils/transformInputData.ts
index 8d4403c..a94946d 100644
--- a/node_modules/victory-native/src/cartesian/utils/transformInputData.ts
+++ b/node_modules/victory-native/src/cartesian/utils/transformInputData.ts
@@ -74,6 +74,7 @@ export const transformInputData = <
     yTicksNormalized: number[];
     yData: Record<string, { i: MaybeNumber[]; o: MaybeNumber[] }>;
   }>;
+  xDataBounds: [number, number];
 } => {
   const data = [..._data];
   const { xAxisScale = "linear", yAxisScale = "linear" } = axisScales || {};
@@ -353,5 +354,7 @@ export const transformInputData = <
     xTicksNormalized: finalXTicksNormalized,
     // conform to type NonEmptyArray<T>
     yAxes: [yAxesTransformed[0]!, ...yAxesTransformed.slice(1)],
+    // Original data bounds (before domainPadding) for lineExtent='data'
+    xDataBounds: [ixMin, ixMax] as [number, number],
   };
 };
diff --git a/node_modules/victory-native/src/index.ts b/node_modules/victory-native/src/index.ts
index 3065639..c10f763 100644
--- a/node_modules/victory-native/src/index.ts
+++ b/node_modules/victory-native/src/index.ts
@@ -28,6 +28,10 @@ export {
   useChartPressState,
   type ChartPressState,
 } from "./cartesian/hooks/useChartPressState";
+export {
+  useChartHoverState,
+  type ChartHoverState,
+} from "./cartesian/hooks/useChartHoverState";
 
 export { useChartTransformState } from "./cartesian/hooks/useChartTransformState";
 export {
diff --git a/node_modules/victory-native/src/types.ts b/node_modules/victory-native/src/types.ts
index a3a4c81..9751079 100644
--- a/node_modules/victory-native/src/types.ts
+++ b/node_modules/victory-native/src/types.ts
@@ -253,6 +253,12 @@ export type YAxisInputProps<
   domain?: YAxisDomain;
   linePathEffect?: DashPathEffectComponent;
   enableRescaling?: boolean;
+  /** Controls the horizontal extent of grid lines. 'content' extends lines to content edges (e.g., bar edges), 'domain' uses full padded domain (default). */
+  lineExtent?: "domain" | "content";
+  /** Inner padding between chart elements (required for lineExtent='content'). Should match Bar component's innerPadding. */
+  contentInnerPadding?: number;
+  /** Number of data points (required for lineExtent='content'). */
+  dataPointCount?: number;
 };
 
 export type YAxisPropsWithDefaults<
@@ -261,13 +267,13 @@ export type YAxisPropsWithDefaults<
 > = Required<
   Omit<
     YAxisInputProps<RawData, YK>,
-    "font" | "tickValues" | "linePathEffect" | "enableRescaling"
+    "font" | "tickValues" | "linePathEffect" | "enableRescaling" | "lineExtent" | "contentInnerPadding" | "dataPointCount"
   >
 > &
   Partial<
     Pick<
       YAxisInputProps<RawData, YK>,
-      "font" | "tickValues" | "linePathEffect" | "enableRescaling"
+      "font" | "tickValues" | "linePathEffect" | "enableRescaling" | "lineExtent" | "contentInnerPadding" | "dataPointCount"
     >
   >;
 
@@ -280,6 +286,8 @@ export type YAxisProps<
   yTicksNormalized: number[];
   yKeys: YK[];
   chartBounds: ChartBounds;
+  /** Original data bounds (before domainPadding) for lineExtent='data' */
+  xDataBounds?: [number, number];
 };
 
 export type FrameInputProps = {
